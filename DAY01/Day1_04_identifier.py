# rule : 식별자는 문자로 시작, 대소문자 구분, 키워드 사용 안됨
# convention : 변수나 함수 소문자(두단어 이상 이어질 때 )
#              클래스 Camelcas(대문자로 시작)

# # l#variable=10
# print(lvariable)

# tax_rate =3.3
# def get_tax()

# 모든 언어는 공통으로 ""(double quotation)내에서 ''(single quotation) 반대 역시 마찬가지
# 그러나 되도록이면 \ 이것을 많이 사용하도록 해라

# 데이터를 하나의 괄호로 묶으면 여러개의 데이터를 하나의 데이터로 인식한다.

