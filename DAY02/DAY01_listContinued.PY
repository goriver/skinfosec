# array = [1,2,3,4,5]
# for data in array:
#     print(data, end='\t') # end=을 안적으면 디폴트로 줄바꿈이 됨


# json
"""
실무에서는 여러 데이터를 json이 dictionary이다.
쪼개지는 데이터 간 통신은 다 json형태라고 이해하면 된다.
"""
# 딕셔너리
#   : 키를 기반으로 값을 저장하는 것
#   : 값(value)는 list, array도 다 써도 된다.
#   : 리스트가 []라면 딕셔너리는 {}을 더 많이 쓴다, 그러나 한 공간의 값을 가져올때는 dic_ex[0]이게 맞다//
#   : dic_ex{0}은 아님


